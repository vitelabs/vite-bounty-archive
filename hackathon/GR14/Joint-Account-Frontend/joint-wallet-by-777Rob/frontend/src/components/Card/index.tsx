type Props = {
	title?: string;
	link?: string;
	icon?: React.ReactNode;
};

const Card = ({ title, icon }: Props) => {
	return (
		<div className="">
			<p></p>
		</div>
	);
};

export default Card;
