import { State } from '../utils/types';
import { connect } from '../utils/globalContext';

type Props = State & {};

const AppHome = ({}: Props) => {
  return (
    <>
    </>
  )
}

export default connect(AppHome)
